---
// cva-based bordered/hover container, ported verbatim from
// bytetech247.com — fully generic, no site-specific styling.
import { cva, type VariantProps } from "class-variance-authority";

const card = cva(
  "rounded-lg border border-border bg-bg-subtle overflow-hidden shadow-card transition-[transform,box-shadow,border-color] duration-200 ease-out motion-reduce:transition-none hover:-translate-y-0.5 hover:border-accent/40 hover:shadow-card-hover",
  {
    variants: {
      padding: {
        none: "",
        sm: "p-2",
        md: "p-4",
      },
    },
    defaultVariants: { padding: "md" },
  },
);

type Props = VariantProps<typeof card> & { as?: string; class?: string };
const { padding, as: Tag = "div", class: className } = Astro.props;
---

<Tag class:list={[card({ padding }), className]}>
  <slot />
</Tag>
