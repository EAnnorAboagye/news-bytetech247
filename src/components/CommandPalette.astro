---
// Placeholder — the real Pagefind-backed search palette lands in Phase 10
// of the build plan. Header.astro's search button (data-command-palette-trigger)
// renders now with no listener attached until then; a click is a
// harmless no-op in the meantime, not an error.
---
